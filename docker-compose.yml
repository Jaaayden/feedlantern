name: feedlantern
services:
  feedlantern:
    image: ghcr.io/jaaayden/feedlantern:${FEEDLANTERN_VERSION:-latest}
    init: true
    restart: unless-stopped
    stop_grace_period: 90s
    shm_size: 1gb
    security_opt:
      - seccomp=./seccomp_profile.json
    ports:
      - "127.0.0.1:4321:4321"
    environment:
      ALLOWED_TARGET_HOSTS: ${ALLOWED_TARGET_HOSTS:-}
      BACKGROUND_CONCURRENCY: ${BACKGROUND_CONCURRENCY:-1}
      DNS_OVER_HTTPS: ${DNS_OVER_HTTPS:-false}
    volumes:
      - data:/app/data
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:4321/api/auth/status',{}).then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
volumes:
  data:
