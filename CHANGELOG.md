# Changelog

## 0.2.0 — 待完成发布验收

- 默认列表视图，支持卡片切换、服务器持久化偏好、选择和批量复制/暂停/恢复/刷新/删除。
- 独立修改阅读器中的 RSS 频道名称，无需重新抓取，保留原订阅地址和历史。
- 批量网址创建支持高可信自动保存、人工确认、取消、重试及服务重启恢复。
- 增加密码加密完整备份、跨主密钥恢复和轻量配置合并；初始化页支持直接恢复。
- 提供非 root Chromium 沙箱 Docker 镜像构建、Compose、Nginx HTTPS 示例和版本化安装脚本。
- README 精简为入口，完整部署、使用、迁移、维护、开发文档集中到 doc/。
- CI 增加双架构原生容器、真实 Nginx HTTPS 和数据持久化验收，标签通过后发布 GHCR 与 Release。

## 0.1.0 — 2026-09-06

- 实现中文管理网页：首次初始化、登录、退出、修改密码及本机重置。
- 支持 Cookie 字符串和 OpenCookie JSON 导入，加密存储和独立浏览器上下文。
- 自动发现重复内容列表，提取标题、链接、摘要、图片和日期，提供候选和订阅预览。
- 支持截图点选、缩放坐标映射、选择器编辑和手动字段保留；只允许保存已完成预览的规则。
- 实现订阅编辑、暂停、恢复、定时与立即刷新、失败保留历史、URL 去重及 RSS 密钥轮换。
- 管理请求校验登录、CSRF、Origin 和 Host，浏览器通过受限代理固定目标 IP，默认阻断私网与保留地址。
- 增加本地 fixture、真实 Chromium 验收和 Node.js 24 CI。
- 修复公网检查发现的日期标签误作标题，以及并发刷新、凭据域绑定、RSS 摘要转义问题。
- 修复管理错误退化为 Bad Request、HTTPS 隧道 EPIPE、正文选择器扩展到页脚；支持可选加密 DNS，兼容本机 Fake-IP 代理环境。
- 通过上科大科研进展和 Pepper 博客的公网自动推荐复验，结果及早期网络限制记录于 `doc/history/public-smoke.md`。镜像、安装包和 Release 自动分发留待后续。

## 0.1.0-dev.1

- 建立 TypeScript、React、Fastify 工程与共享接口。
- 确定自动识别优先、可视化纠偏、单管理员登录和 Cookie 导入的首版范围。
