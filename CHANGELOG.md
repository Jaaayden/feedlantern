# Changelog

## v0.5.1

- 每个用户可独立设置 Bark 地址、开关、失败阈值与重试参数，并向明确显示的接收账号发送测试通知；故障只通知订阅所有者，不回退管理员地址。
- 用户管理新增改名、角色升降级和永久删除；删除前展示资源数量并要求输入用户名。所有管理员同权，事务保护最后一名启用的管理员，禁止停用或删除自己。
- 管理员可进入任意账号的订阅、凭据、批量任务和个人通知工作台，持续显示当前目标；停用用户仅允许查看和修改静态配置。
- 队列及浏览器操作校验操作人和所有者，身份变化撤销会话并取消相关操作；切换工作台丢弃旧响应，Cookie 与 Bark 密钥不回显。
- 数据库升级至版本 8，旧全局 Bark 迁移给原管理员；完整备份升级至 version 3，兼容旧备份、多管理员及个人通知，保留已有 RSS 地址。
- 整站备份验证当前管理员密码；本机 `admin:reset` 在多管理员时要求选择目标。更新 API、端到端测试及使用维护文档。

升级前请备份完整数据卷；回退旧程序时同时恢复旧数据卷。轻量配置只包含操作管理员自己的订阅与通知密钥，请私密保存。

## v0.5.0

- 新增管理员创建、停用／启用普通用户及重置密码；所有用户使用统一登录页并可修改自己的密码。
- 订阅、Cookie 凭据、批量任务及浏览器编辑会话按用户隔离，管理员工作台也只显示自己的数据。
- 系统设置和整站备份仅管理员可用；修改密码只撤销目标账号会话，账号切换时丢弃旧请求响应。
- 停用账号撤销登录、关闭编辑会话、取消后台操作并暂停 RSS 访问；重新启用恢复待处理任务，保留手动暂停的订阅。
- 数据库迁移至版本 7，完整备份升级至 version 2 并兼容旧备份，保留原管理员密码、订阅历史及 RSS 地址。
- 新增用户权限、并发停用、账号切换、迁移与端到端测试，并更新使用和维护文档。

升级提示：升级前备份完整数据卷。升级后需重新登录；原数据归原管理员。回退旧版程序时须同时恢复升级前数据卷。

## v0.4.1

- 支持 Science 等网站提供的 RSS 1.0（RDF）订阅，读取 RDF 条目标识、正文与 Dublin Core 日期，修复有效订阅被误判为非 RSS 的问题。

## v0.4.0

- 新增 RSS/Atom 中文与双语订阅，支持批量添加、后台翻译、持久化缓存和失败重试。
- 使用 Google 浏览器翻译接口，原生数组批量提交并发请求；只有标题和正文均完成的条目才进入公开订阅。
- 网页抓取订阅可选择原文、中文或双语输出，翻译已有标题与摘要，保留订阅地址、历史和图片。
- 订阅列表区分来源及输出模式，支持按来源和是否翻译筛选。
- 增加实时翻译进度与日志、限流退避状态、手动重试和翻译失败 Bark 告警；Bark 设置增加测试通知按钮。
- 数据库自动升级至版本 6，已有网页订阅保持原文；支持配置与完整备份迁移。
- 修复批量任务结束时立即重试可能不唤醒队列的问题。

升级提示：Google 免费浏览器翻译通道仍为实验性，部署主机需能访问 `translate-pa.googleapis.com`。升级前请导出加密备份；新数据库及备份不支持直接降级到旧程序。

## v0.3.0

- 增加订阅抓取日志：记录所有实际抓取的来源、结果、耗时和新增条目数，支持筛选、分页、自动更新与重启中断恢复；默认保留 30 天。
- 增加 Bark 故障通知：首次失败告警、连续失败去重、后台重试及持久化恢复，不因没有新文章误报。
- 网页统一管理 Bark、日志、并发、网络白名单、DNS、可信代理及服务器设置；监听地址与端口重启生效。
- 应用设置随配置导入导出与完整备份迁移，Bark 地址在数据库中加密保存；普通配置文件包含 Bark 密钥，需私密保管。旧环境配置首次自动迁入网页设置。
- 恢复网页上下按钮滚动，改善论坛日期识别。

- 拆分订阅详情与设置入口，明确匹配规则编辑，设置支持直接修改名称和刷新间隔，无需重新预览网页。
- 修改间隔后重新计算下次检查时间，保留暂停状态及历史，避免旧抓取或调度覆盖设置；未保存修改提供离开提醒。

- 日期点选支持从分类、来源等混合文本中提取时间；相对时间标记为估算，保存后不随刷新漂移，准确时间可纠正估算。
- 霂明导航 `/intel` 按文章链接读取原始时间戳；统一日期预览、自动识别和后台抽取，兼容旧数据库和备份。

## v0.2.2

- 本机 Nginx 反代自动识别域名与 HTTPS，无需 PUBLIC_ORIGIN 或 .env；修复默认部署下域名访问被拒绝的问题。
- 保留来源校验，隔离并发请求的 RSS 地址与 Cookie 配置；简化 Nginx 文档为模板，移除证书申请说明。

## v0.2.1

- 统一订阅列表名称与 RSS 频道标题，改名不改变地址、历史和调度；兼容旧数据及备份中的自定义频道名称。
- README 改为直接下载 Docker Compose 配置并启动，移除一行安装脚本；首次设置码在服务启动日志中显示。


## 0.2.0 — 2026-09-07

- 默认列表视图，支持卡片切换、服务器持久化偏好、选择和批量复制/暂停/恢复/刷新/删除。
- 独立修改阅读器中的 RSS 频道名称，无需重新抓取，保留原订阅地址和历史。
- 批量网址创建支持高可信自动保存、人工确认、取消、重试及服务重启恢复。
- 增加密码加密完整备份、跨主密钥恢复和轻量配置合并；初始化页支持直接恢复。
- 提供非 root Chromium 沙箱 Docker 镜像构建、Compose、Nginx HTTPS 示例和版本化安装脚本。
- README 精简为入口，完整部署、使用、迁移、维护、开发文档集中到 doc/。
- CI 增加双架构原生容器、真实 Nginx HTTPS 和数据持久化验收，标签通过后发布 GHCR 与 Release。

## 0.1.0 — 2026-09-06

- 实现中文管理网页：首次初始化、登录、退出、修改密码及本机重置。
- 支持 Cookie 字符串和 OpenCookie JSON 导入，加密存储和独立浏览器上下文。
- 自动发现重复内容列表，提取标题、链接、摘要、图片和日期，提供候选和订阅预览。
- 支持截图点选、缩放坐标映射、选择器编辑和手动字段保留；只允许保存已完成预览的规则。
- 实现订阅编辑、暂停、恢复、定时与立即刷新、失败保留历史、URL 去重及 RSS 密钥轮换。
- 管理请求校验登录、CSRF、Origin 和 Host，浏览器通过受限代理固定目标 IP，默认阻断私网与保留地址。
- 增加本地 fixture、真实 Chromium 验收和 Node.js 24 CI。
- 修复公网检查发现的日期标签误作标题，以及并发刷新、凭据域绑定、RSS 摘要转义问题。
- 修复管理错误退化为 Bad Request、HTTPS 隧道 EPIPE、正文选择器扩展到页脚；支持可选加密 DNS，兼容本机 Fake-IP 代理环境。
- 通过上科大科研进展和 Pepper 博客的公网自动推荐复验，结果及早期网络限制记录于 `doc/history/public-smoke.md`。镜像、安装包和 Release 自动分发留待后续。

## 0.1.0-dev.1

- 建立 TypeScript、React、Fastify 工程与共享接口。
- 确定自动识别优先、可视化纠偏、单管理员登录和 Cookie 导入的首版范围。
